# Server Module

This is the JVM service module for Legado Server. Build, run, data-layout, Docker, systemd, security, and capability documentation live in the repository [README](../README.md).

The module bundles `src/main/resources/web`, which is produced from `modules/web` with `pnpm build`.
